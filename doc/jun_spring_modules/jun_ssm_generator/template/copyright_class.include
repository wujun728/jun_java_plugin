/**
 * TODO 在此加入类描述
 * @copyright {@link weicms.net}
 * @author springrain<Auto generate>
 * @version <#if now??> ${now?string('yyyy-MM-dd HH:mm:ss')}</#if>
 * @see ${basepackage}.${myParentDir}.${className}
 */
 