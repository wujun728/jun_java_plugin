<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.chentongwei.dao.common.IUserDAO">

	<sql id="basic_column">
		id,
		login_name AS loginName,
		gender,
		birthday,
		avatar,
		email,
		status,
		open_id AS openId,
		access_token AS accessToken,
		is_admin AS isAdmin,
		create_time AS createTime
	</sql>
	
	<select id="login" parameterType="loginIO" resultType="userVO">
		SELECT
			<include refid="basic_column" />
		FROM
			dt_user
		WHERE
			login_name = #{loginName}
		AND
			password = #{password}
	</select>

</mapper>