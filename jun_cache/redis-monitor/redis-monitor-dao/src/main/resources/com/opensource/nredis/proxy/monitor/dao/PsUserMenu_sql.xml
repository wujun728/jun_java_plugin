<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!--
IPsUserMenuDao mybatis

@author liubing
@date 2016/12/20 18:45
@version v1.0.0
-->
<mapper namespace="com.opensource.nredis.proxy.monitor.dao.IPsUserMenuDao" >
    <resultMap id="PsUserMenuMap" type="com.opensource.nredis.proxy.monitor.model.PsUserMenu">
        <result property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="menuId" column="menu_id"/>
    </resultMap>

    <insert id="insert" parameterType="com.opensource.nredis.proxy.monitor.model.PsUserMenu">
            <selectKey keyProperty="id" resultType="Integer" order="AFTER">
                SELECT LAST_INSERT_ID() AS id
            </selectKey>
        INSERT INTO ps_user_menu (
        <trim suffixOverrides=",">
                id,
                user_id,
                menu_id,
        </trim>
        )VALUES(
        <trim suffixOverrides=",">
                    #{id},
                    #{userId},
                    #{menuId},
        </trim>
        )
    </insert>

    <update id="updateById" parameterType="com.opensource.nredis.proxy.monitor.model.PsUserMenu">
        update ps_user_menu
        <trim prefix="SET" suffixOverrides=",">
            <if test="id != null">id = #{id},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="menuId != null">menu_id = #{menuId},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteById" parameterType="int">
        delete from ps_user_menu where id = #{id}
    </delete>

    <select id="getById" parameterType="int" resultMap="PsUserMenuMap">
        select * from ps_user_menu where id = #{id}
    </select>

    <select id="getListByCriteria" resultMap="PsUserMenuMap" parameterType="com.opensource.nredis.proxy.monitor.model.PsUserMenu">
        select * from ps_user_menu
        <include refid="select_where_clause"/>
        order by id desc
    </select>

    <sql id="select_where_clause">
        <trim prefix="where" prefixOverrides="AND|OR">
            <if test="id != null"> AND id = #{id} </if>
            <if test="userId != null"> AND user_id = #{userId} </if>
            <if test="menuId != null"> AND menu_id = #{menuId} </if>
        </trim>
    </sql>
    
    <select id="queryPageList" resultMap="PsUserMenuMap">
        select * from ps_user_menu
        <include refid="page_where_clause"/>
        order by id desc
        <if test="page!=null">
            LIMIT #{page.pageSize} OFFSET #{page.begin}
        </if>
    </select>

    <select id="count" resultType="int" parameterType="com.opensource.nredis.proxy.monitor.model.PsUserMenu">
        select count(*) from ps_user_menu
        <include refid="page_where_clause"/>
    </select>

    <sql id="page_where_clause">
        <trim prefix="where" prefixOverrides="AND|OR">
            <if test="queryObject.id != null"> AND id = #{queryObject.id} </if>
            <if test="queryObject.userId != null"> AND user_id = #{queryObject.userId} </if>
            <if test="queryObject.menuId != null"> AND menu_id = #{queryObject.menuId} </if>

           
        </trim>
    </sql>

    <!--extension script-->
</mapper>