1.增加队列健康检测,队列流畅度,耗时
2.task增加最大运行时长限制,通过wroker标记运行时间检测,并在特殊队列循环执行判断,通过中断线程实现
3.抽离线程池逻辑,使得可以使用JDK自带线程池或者自定义线程池
4.基于注解和拓展参数配置类实现替换task为jdk runnable来达到特殊逻辑的处理
5.通过warp包装模式实现executor变为ScheduledExecutorService
5.queueGroup的拓展接口可以执行和调度runnable,即M个线程处理N个队列的任务的同时,还处理直接调度的runnable,处理时就相当于处理一个只含有一个任务的队列,即也会占用线程,如果当前所有线程都在处理非队列任务(活动线程数量降低),则会触发新线程启动