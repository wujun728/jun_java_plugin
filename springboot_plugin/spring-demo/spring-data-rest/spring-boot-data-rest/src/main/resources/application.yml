server:
  port: 8081
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/test?useUnicode=true&characterEncoding=utf-8
    username: services_dev
    password: services_dev
    driver-class-name: com.mysql.jdbc.Driver
  data:
    rest:
      base-path: /api/v1 #指定restful的前缀
