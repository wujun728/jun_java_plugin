server:
  port: 8080
spring:
  datasource:
    url: jdbc:h2:mem:sc-test;DB_CLOSE_ON_EXIT=FALSE
    username: root
    password: root
    driver-class-name: org.h2.Driver
  application:
    name: spring-boot-test
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    database-platform: H2
    database: h2
