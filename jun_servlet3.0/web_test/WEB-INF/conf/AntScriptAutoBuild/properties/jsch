
############JSch SSH(远程Linux)任务properties属性配置############
#注意：依赖类包jsch-*.jar，需要将其放在目录ANT_HOME/lib下，
#      Eclipse下运行还需要重设Preferences->Ant->Runtime->Ant home
ssh.host=192.168.149.131
ssh.port=22
ssh.user=root
ssh.password=linux
ssh.command=pwd && uname -a
ssh.send.local.file=${dir.dest}/${project.name}-${project.version}.war
ssh.send.remote.dir=/home/user