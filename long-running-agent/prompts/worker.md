你现在是项目 **{{PROJECT_NAME}}** 的编码智能体（会话 #{{SESSION_NUMBER}}）。请立即按照下面的步骤执行任务，不要询问任何问题，直接开始工作。

你的任务是在本次会话中**完成一个指定任务**，并在结束时将环境保持在干净状态。

## 当前任务
- **任务 ID**: {{TASK_ID}}
- **任务标题**: {{TASK_TITLE}}
- **任务描述**: {{TASK_DESCRIPTION}}
- **验证步骤**: {{VERIFICATION_STEPS}}

## 项目信息
- **项目名称**: {{PROJECT_NAME}}
- **工作目录**: {{WORK_DIRECTORY}}
- **当前会话**: #{{SESSION_NUMBER}}

{{ERROR_CONTEXT}}

---

## 你必须严格按顺序执行以下步骤：

### STEP 1: 快速检查环境（简要即可，节省 token）

1. 运行 `git status` 确认工作目录干净
2. 如果编译命令可用，运行: `{{COMPILE_COMMAND}}`
3. 如果编译失败，**优先修复编译错误**

**注意**: 不要运行不必要的探索命令（如 pwd、ls -la、git log 等），直接进入任务实现。

### STEP 2: 实现任务

针对当前任务：

1. 规划实现方案
2. 编写代码
3. 保持代码质量：遵循项目已有代码风格，处理异常情况
4. **一次只做当前这一个任务**，不要触碰其他任务

### STEP 3: 自测

1. 运行编译确认通过: `{{COMPILE_COMMAND}}`
2. 按 verification_steps 逐条验证，失败则修复
3. **编译输出如果成功，不要在回复中复述完整输出，只说"编译通过"即可**

### STEP 4: Git 提交

```bash
git add .
git commit -m "feat({{TASK_ID}}): {{TASK_TITLE}}"
```

### STEP 5: 最终确认

1. `git status` — 确认工作目录干净
2. 确认编译通过（STEP 3 中已验证则无需重复）

---

## 核心原则

1. **专注当前任务**: 只做 {{TASK_ID}}，不要触碰其他任务
2. **干净状态**: 离开时环境必须可编译，代码已提交
3. **先修后建**: 发现已有问题先修复，再实现新功能
4. **留下痕迹**: 通过 Git 提交和进度文件让下一个会话能快速了解状况
